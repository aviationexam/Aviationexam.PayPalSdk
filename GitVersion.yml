workflow: TrunkBased/preview1
branches:
  develop:
    regex: ^master$|^main$|^develop$
    increment: Patch
    label: nightly
    source-branches: [develop]
ignore:
  sha: []
merge-message-formats: {}
